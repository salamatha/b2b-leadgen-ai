
/**
 * Example helper to call OpenAI - replace with official SDK usage
 * This file shows a conceptual approach; install and configure the OpenAI SDK in production.
 */
export async function parseHtmlWithOpenAI(html: string) {
  // Use OpenAI SDK to call model with a JSON schema and return parsed result.
  // Placeholder - implement with openai.chat.completions or client.chat.completions.create
  return { company_name: null, emails: [] }
}
